# LibreCode
LibreCode is a programming language that will be made like an assembler and high level too

Comands Avaliable:

/ Comments

mov(rax, 10)
mov(rax, rdx)
mov(rax, [VarImediate])
mov(rax, &VarAddress)
mov([Var], rax)

var x[1] = 10
var hi[2] = "Hi"
var buffer[1024]

exec() 

exit(Code) 
exit(10)
exit() // Without code equals 0

Registrators avaliable:

rax
rdi
rsi
rdx
rcx
rbx
al
bl
cl
dl
